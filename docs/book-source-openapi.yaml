openapi: 3.1.0
info:
  title: Open Reading Source Protocol
  version: 1.0.0
  description: Standard API implemented by an Open Reading compatible book source.
servers:
  - url: https://books.example.org/api
paths:
  /v1/search:
    get:
      operationId: searchBooks
      parameters:
        - name: q
          in: query
          required: true
          schema: { type: string, minLength: 1, maxLength: 200 }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        '200':
          description: Search results
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SearchPage' }
  /v1/books/{bookId}:
    get:
      operationId: getBook
      parameters:
        - $ref: '#/components/parameters/BookId'
      responses:
        '200':
          description: Book details
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Book' }
        '404': { $ref: '#/components/responses/NotFound' }
  /v1/books/{bookId}/chapters:
    get:
      operationId: listChapters
      parameters:
        - $ref: '#/components/parameters/BookId'
      responses:
        '200':
          description: Chapter catalog
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/Chapter' }
  /v1/books/{bookId}/chapters/{chapterId}:
    get:
      operationId: getChapterContent
      parameters:
        - $ref: '#/components/parameters/BookId'
        - name: chapterId
          in: path
          required: true
          schema: { type: string, minLength: 1, maxLength: 500 }
      responses:
        '200':
          description: Chapter content
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ChapterContent' }
        '404': { $ref: '#/components/responses/NotFound' }
components:
  parameters:
    BookId:
      name: bookId
      in: path
      required: true
      schema: { type: string, minLength: 1, maxLength: 500 }
  responses:
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema: { $ref: '#/components/schemas/ErrorResponse' }
  schemas:
    Book:
      type: object
      required: [id, title]
      properties:
        id: { type: string, minLength: 1 }
        title: { type: string, minLength: 1 }
        author: { type: string }
        description: { type: string }
        coverUrl: { type: string, format: uri }
        categories:
          type: array
          items: { type: string }
        status: { type: string, enum: [ongoing, completed, hiatus, unknown] }
        latestChapter: { type: string }
        updatedAt: { type: string, format: date-time }
    SearchPage:
      type: object
      required: [items, page, pageSize, hasMore]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/Book' }
        page: { type: integer, minimum: 1 }
        pageSize: { type: integer, minimum: 0 }
        total: { type: integer, minimum: 0 }
        hasMore: { type: boolean }
    Chapter:
      type: object
      required: [id, title, order]
      properties:
        id: { type: string, minLength: 1 }
        title: { type: string, minLength: 1 }
        order: { type: integer, minimum: 0 }
        updatedAt: { type: string, format: date-time }
    ChapterContent:
      type: object
      required: [bookId, chapterId, title, contentType, content]
      properties:
        bookId: { type: string, minLength: 1 }
        chapterId: { type: string, minLength: 1 }
        title: { type: string, minLength: 1 }
        contentType:
          type: string
          enum: [text/plain, text/markdown, text/html]
        content: { type: string }
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string }
            message: { type: string }
