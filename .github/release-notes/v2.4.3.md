# Open Reading v2.4.3

v2.4.3 是 2.4.2 的快速修复版本。

## 修复

- **切换 AI 模型即时生效**：修复在设置里切换 AI 模型后，已打开的「问AI」面板与 AI 对话页仍使用旧模型的问题。对话界面现在每次发送都读取当前激活的模型与配置，无需重启应用。

## 验证

- 全量 `flutter test` 通过；`flutter analyze` 无 error/warning；`dart format` 检查 0 变更。

## 版本

- 版本：`2.4.3`
- 基础构建号：`260727002`
